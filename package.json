{
  "name": "itsnwa.com",
  "private": true,
  "scripts": {
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && gridsome build",
    "develop": "export NODE_OPTIONS=--openssl-legacy-provider && gridsome develop",
    "explore": "export NODE_OPTIONS=--openssl-legacy-provider && gridsome explore"
  },
  "dependencies": {
    "@gridsome/remark-prismjs": "0.3.0",
    "@gridsome/source-filesystem": "^0.6.2",
    "@gridsome/transformer-remark": "^0.5.0",
    "gridsome": "^0.7.19",
    "package.json": "^2.0.1"
  }
}
